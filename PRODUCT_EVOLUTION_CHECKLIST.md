# With You 产品演进清单

> 用途：持续记录已经确认的产品决策、遗留问题和后续优化方向，避免长期迭代时遗忘上下文。
>
> 最近更新：2026-07-26

## 状态说明

- `[x]` 已完成
- `[~]` 已有基础实现，仍需继续完善
- `[ ]` 尚未开始
- `[?]` 需要讨论或验证

## 已确认的核心架构

- `[x]` 文件树是小说项目的事实来源和长期记忆载体。
- `[x]` 写作会话、创作工具和文件树之间已经形成读写闭环。
- `[x]` 每个会话都是独立创作空间；新会话不属于前一个作品，严禁继承前项目记忆。
- `[x]` 创作工具模型、会话写作模型、封面模型和 Pi 模型使用独立路由，互不串用。
- `[x]` 提示词市场已经接入对应 Agent 管道，并支持应用、编辑和恢复默认。
- `[x]` 故事世界图谱采用题材无关的舞台/区域命名，不绑定现实地图名称。
- `[x]` 故事图谱已升级为“阶段星球总览 → 阶段内大陆画布”：最多十个叙事阶段按主线箭头连接，星球采用行星化视觉；进入阶段后使用 React Flow 无限画布，以不规则大陆板块分散承载人物、势力、关系与关键事件（2026-07-23）。
- `[x]` 图谱已采用作品目录内 `vault/story-graph.json` v3 持久化与三代恢复点；AI 提取默认进入候选层，只有用户确认且可见、未过期的事实进入任务级项目知识；删除改为隐藏，不再隐式删除实体、时间线或正文，并支持冲突标记、驳回、恢复与损坏文件隔离。已加入 WorldMonitor 式集中图层注册、按章节/图层渐进展示和 URL 视图状态；生产构建与安全测试已验收通过（2026-07-26）。
- `[x]` 图谱采用多对多关系模型：任意 A—B、A—C、B—C 及更多节点对均可独立连线，同一节点对允许多条不同语义的关系；新增节点默认孤立。关系通过节点边缘连接手柄拖拽创建，阶段内画布支持无限平移、滚轮缩放、连线重接、编辑与删除；单节点最多 200 条直接关系（2026-07-23）。
- `[x]` Pi 使用真实外部 Coding Agent，并通过白名单工具操作项目。
- `[x]` Pi 权限分为观察、项目、源码三级；所有写入先形成候选改动。
- `[x]` 源码权限需要明确授权，30 分钟后自动失效，支持检查点和回滚。
- `[x]` Pi 模型已独立配置并完成真实模型、流式响应和工具调用验收。
- `[x]` Pi 悬浮入口支持拖动、屏幕边界限制和本地位置记忆。

## Pi 后续事项

- `[ ]` 设计 Pi Skill 管理器。
- `[ ]` 内置可信 Skill：随软件发布，可直接启用。
- `[ ]` 用户安装 Skill：安装前展示文件、网络、命令和源码权限清单。
- `[ ]` 源码维护 Skill：仅在三级权限临时解锁期间可使用。
- `[ ]` Skill 默认禁止任意 Shell、项目外文件读取和静默联网。
- `[ ]` 为 Skill 建立签名、来源、版本、更新和卸载机制。
- `[ ]` 为 Pi 增加会话压缩、阶段性审查和手动全局审查节点。
- `[ ]` EXE 打包时将源码维护运行时拆为独立 sidecar，避免 Next.js 动态工作区追踪。

## Web 化 UI 方向

### 当前界面的优点

- 信息密度适合桌面写作，不廉价，也没有过度装饰。
- 左侧功能区、中部创作区、右侧文件树的职责容易理解。
- 黑白灰主视觉稳定，适合长时间使用。
- 功能入口基本完整，已经形成产品而不是演示页面。

### 当前产生“略显简陋”的原因

- `[ ]` 三个区域主要依靠细边框分隔，空间层级较平，缺少明确的工作区层次。
- `[x]` 中部空状态已增加当前作品、创作阶段、字数和下一步建议（2026-07-23）。
- `[x]` 左侧已区分核心创作流程与低频“工作台”功能（2026-07-23）。
- `[x]` 右侧文件树在无项目时使用导入/立项空状态，不再预生成大量灰色章节（2026-07-23）。
- `[ ]` 设置、工具、Pi 等功能主要以叠层面板出现，Web 场景下会削弱页面导航感和可分享性。
- `[x]` URL 已表达当前独立会话和创作工具；作品绑定仍由会话的服务端所有权记录表达（2026-07-23）。

### 建议的视觉升级原则

- `[ ]` 保留当前克制的黑白灰风格，不做大面积渐变或卡片堆砌。
- `[ ]` 建立三层表面：应用背景、工作区表面、临时浮层；用极轻的明度和阴影区分。
- `[x]` 已增加稳定作品头部：书名、章节数、字数、保存/生成状态和创作阶段（2026-07-23）。
- `[ ]` 将“大纲 → 细纲 → 章节”的因果工作流提升为一级视觉路径。
- `[x]` 已把统计、模型设置、提示词市场等低频功能收进“工作台”，减少左侧同权入口（2026-07-23）。
- `[~]` 中部空状态已有继续写作和上下文感知建议；最近编辑、待办节点和项目健康提示仍待补充。
- `[ ]` 统一按钮高度、图标尺寸、圆角、悬停和选中状态，形成稳定的设计 token。
- `[ ]` 增加紧凑/舒适两种密度，兼顾桌面 EXE 和浏览器。

### Web 响应式与路由

- `[ ]` 桌面宽屏：保留左侧导航 + 中部工作区 + 右侧文件树。
- `[ ]` 普通笔记本：左右栏可独立折叠，中部保持最小写作宽度。
- `[ ]` 平板：左侧改为抽屉，文件树改为可切换侧栏。
- `[ ]` 手机：只保留单栏编辑与底部主要导航，不强行缩小三栏。
- `[x]` 独立会话已映射到 `/studio/session/[conversationId]`；作品绑定由会话及服务端租约共同表达（2026-07-23）。
- `[x]` 创作工具已使用 `/studio/session/[conversationId]/tools/[toolId]`，刷新后可恢复（2026-07-23）。
- `[~]` 创作工具已有可持久路由；设置、提示词详情和图谱仍需从工作区面板进一步升级为独立页面。
- `[ ]` 为 Web 版本补充鉴权、云端同步、冲突处理、上传限制和断线恢复。

## “开启新会话”改造记录

### 已修复的旧实现

- 已拆除“单击新建 + 悬停一秒打开历史 + 四秒自动关闭”的冲突入口。
- 新建与历史浏览已拆成固定侧栏中的明确操作。
- 会话对象保存消息、完整作品草稿并可绑定唯一的 `novelId`；这是项目隔离边界，不应拆成前作品下的普通聊天线程。
- 前端草稿、文件树缓存、工具上下文、Pi 会话与后端项目访问均按会话隔离。
- 删除或归档当前会话会确认并切换到安全回退会话，没有其他会话时创建空白会话。
- 历史列表已有当前选中态、重命名、置顶、搜索、归档和删除确认。

### 已确认的目标模型

会话本身就是独立创作工作区，而不是前一个作品下面的聊天线程：

1. **独立会话**
   - 拥有自己的消息、作品草稿、文件树绑定、Agent 记忆和可选 `novelId`。
   - 一个新会话必须从空白状态开始，不自动绑定或读取前一个作品。
   - 导入小说或完成立项后，作品目录只绑定到当前会话。

2. **严格隔离范围**
   - 新会话的 `messages`、`novelId`、草稿、章节、工具上下文和 Pi 项目会话全部清空。
   - 项目级提示词、实体、伏笔、文件树缓存和检索上下文不得沿用。
   - 只允许保留真正的全局设置：模型配置、界面偏好和经过授权的应用级能力。
   - 切回旧会话时，再恢复该旧会话自己的作品和项目记忆。

### 建议的新交互

- `[x]` 采用 Codex 式固定会话侧栏，直接展示独立创作会话，不使用悬停浮框。
- `[x]` “+ 新会话”立即进入全新的空白创作空间，并断开前作品的全部项目上下文。
- `[x]` 导入作品属于当前空白会话的立项方式，而不是切换回前作品。
- `[x]` 当前会话有明确选中态，并显示作品名、更新时间或绑定状态。
- `[x]` 删除当前会话前要求确认；删除后切换到最近的其他独立会话，没有时创建全新空白会话。
- `[x]` 支持会话重命名、搜索、置顶和归档。
- `[x]` 新会话正式产生第一条消息、导入或立项操作后再进入历史列表，避免积累误触产生的空记录。

### 推荐实施顺序

- `[x]` 第一步：拆掉“单击新建 + 悬停历史”的双重行为，改成两个明确入口。
- `[x]` 第二步：新会话切换统一清空项目级状态，并由服务端租约阻止跨会话项目访问。
- `[x]` 第三步：修复删除当前会话后的工作区同步和安全回退。
- `[x]` 第四步：将独立会话写入 URL，为 Web 化做准备。
- `[~]` 第五步：已增加重命名、搜索、置顶和归档；会话模板尚未实现。

## 灰度测试前建议

- `[~]` 空白会话已提供配置模型、生成角色、生成大纲和导入作品入口；仍需补充完整的首次使用引导与完成度反馈。
- `[x]` 创作工具会显示生成依赖和本次自动引用的信息来源。
- `[~]` 会话、Pi 和提示词等关键链路已有可执行错误提示；仍需统一覆盖全部 API 与错误码。
- `[x]` 已统一展示正在生成、正在保存、已保存、保存失败和 Pi 候选改动状态（2026-07-23）。
- `[x]` 文件树作品头部已增加项目备份导出和合并恢复入口；恢复前自动建立服务端恢复点（2026-07-23）。
- `[x]` 模型设置页已增加最小隐私与权限边界说明（2026-07-23）。
- `[ ]` 用真实长篇项目进行 Token、上下文压缩、跨章节一致性和大文件导入压力测试。

## 规模化对抗审查（2026-07-23）

> 结论：当前作品隔离与常规创作链路已有基础，但以下 P0 未关闭前，不应邀请外部灰度用户或交付可联网版本。

### P0：发布阻断

- [x] 已重写旧版 `/api/novels/files` 与 `NovelFileSystem` 的路径边界：项目 ID 严格规范化，所有文件操作统一进行词法与真实路径校验，并覆盖 `..`、绝对路径、UNC 与 Windows 盘符路径回归测试（2026-07-25）。
- [x] Pi 源码权限的 `/api/pi/source*` 已补本地用户认证、会话租约、本机同源限制和一次性随机授权令牌；解锁口令不再由接口返回，授权可撤销且超时失效（2026-07-25）。
- [x] `settings.json`、`.data/`、`.pi/` 与运行时小说数据已纳入根目录忽略规则；Git 当前索引与历史未发现这些路径的跟踪记录，因此没有仓库层面的密钥轮换依据（2026-07-25）。
- [x] `next` 已升级到 16.2.11，直接 `undici` 为 8.9.0，易受攻击的传递依赖已覆盖到修复版本；`pnpm audit --prod --audit-level high` 为 0 个 high（2026-07-25）。

### P1：灰度前应完成

- [x] AI、导入、上传与备份接口已实施服务端频率和请求体限制，模型网关已真正接入每通道频率与并发限制（2026-07-25）。
- [x] 小说导入已限制请求总量、章节数、正文总字节与单章字节；恢复点最多保留 10 份/100MB，客户端只接收恢复点 ID（2026-07-25）。
- [x] 本地章节读取、模型设置写入及上传接口已补工作区租约授权；上传仅接受 JPEG/PNG/WebP，并以服务端魔数检测结果决定扩展名（2026-07-25）。
- [x] 项目文本、导入文本、图谱证据与检索上下文均以不可信数据边界传给模型；Pi 写入继续采用结构化参数和用户候选确认，普通工具写入需用户确认（2026-07-25）。

### P2：交付质量与回归

- [~] 设置文件已统一使用临时文件完整写入、`fsync` 与原子替换，保留三代有效备份；主文件损坏时会保留损坏现场并从最近有效备份自动恢复。异常中断测试按统一验证计划后置。（2026-07-26）
- [ ] 将 Pi 源码运行时拆为 EXE sidecar，消除 Next.js 构建的 whole-project trace 警告；middleware 已迁移为 Next 16 `proxy.ts`。
- [ ] 建立安全回归集：跨会话、跨工作区、路径穿越、上传伪造、Pi 三级授权、提示词注入和重复恢复点均需自动化验证。
- [~] 认证入口已统一为 `/auth/login`、`/auth/register`、`/auth/verify-email` 与 `/auth/callback`；旧 `(auth)/v2` 页面和空 `auth-new` 目录已移除，首页、侧栏登出与回调统一使用认证路由常量。本地模式仍直接进入工作室，Web 认证表单与统一验证后关闭此项。（2026-07-26）

### 长篇上下文与动态压缩专项

> 审查结论：当前实现更接近“超限后截断文本”，尚不足以支撑百万字长篇。小说事实不得依赖会话摘要维持；会话摘要只保存协作过程，文件树与结构化 Vault 才是作品事实源。

#### P0：上下文正确性

- [x] 压缩后会用压缩后的 `novelData` / `vaultContext` 重建最终系统提示词，不再报告未实际作用于模型输入的压缩结果（2026-07-25）。
- [x] 最终网关请求会按真实系统提示词、消息和安全策略重新估算输入 token，并从模型上下文中扣除实际输出预算；仍超预算时返回 `CONTEXT_BUDGET_EXCEEDED`，不提交模型（2026-07-25）。
- [x] 已移除旧消息前 50/100 字拼接方案，改为持久化的结构化会话检查点，保存用户偏好、确认/否决、当前任务、未完成事项、事实变更候选与最后承诺（2026-07-25）。
- [x] 原始消息继续完整保存；检查点作为可重建缓存，记录覆盖消息 ID、来源校验值、生成器标识和生成时间（2026-07-25）。

#### P1：百万字作品的分层记忆

- [~] 已建立每个 `novelId` 独立的小说记忆库，分为作品规范记忆（canonical）、长期记忆和短期记忆；当前以项目内 `vault/memories.json` 落盘，代码已接通，等待统一验证后关闭。（2026-07-26）
- [~] 章节保存后已异步提取章节因果摘要、人物状态、关系、地点、势力、能力物品、伏笔与冲突，并自动分化为长/短记忆；短记忆按章节 TTL 失效，章节重写会保留旧版本并标记 superseded。（2026-07-26）
- [~] 文件树中的核心设定、世界观、角色、金手指、总纲和细纲已同步为作品规范记忆；从正文推断出的规范记忆只进入 candidate，未经确认不会参与写作召回。（2026-07-26）
- [~] 已接通 `memory-retriever` 与写作 `/api/chat`：依据最近写作要求、目标章节、实体关联、时间距离和重要性进行直接召回与关系扩展，不再为写下一章扫描全书正文。（2026-07-26）
- [~] 每条派生记忆已记录来源类型、章节/文件路径、原文证据、内容哈希、置信度、状态和版本时间；正文与用户手写设定继续作为事实源。（2026-07-26）
- [~] 已提供作品记忆查询与修改接口，可确认 candidate、调整层级或停用错误记忆；可视化审查与批量操作界面尚未实现。
- [~] 已增加每部小说独立的可重建 SQLite FTS5 派生索引，并将 BM25 名次通过 RRF 与关键词、实体扩展、时间和重要性重排融合；索引损坏会隔离重建，失败自动退回 JSON 检索，备份排除二进制索引。JSON 仍为事实源，真正的 embedding/向量召回仍待实现。
- [~] 已为导入的既有长篇增加按章节内容哈希快照的批量建库：支持启动、分批运行、暂停、继续、失败退避与手动重试，处理中章节变更会丢弃旧结果并按新快照重排；`content_json` 导入和项目备份恢复后自动创建任务。任务、章节记忆和 Dream 汇总均校验 `novel_id` 并只读取目标作品目录，导入包夹带的跨作品记录不会进入召回。批处理控制界面与统一验证后置。（2026-07-26）
- [~] 已实现小说版 Dream：每 5 章生成剧情块、每 10 章生成阶段汇总，优先读取章节级记忆和上一阶段承接，仅在记忆缺失时读取本区间章节兜底摘录；任务记录输入哈希、状态、尝试次数、退避时间、过期锁和输出版本，章节重写只重建受影响区间，派生文件采用原子写入。token 用量记录与统一验证尚未完成。（2026-07-26）
- [ ] 严格区分两套记忆：会话压缩负责用户偏好、临时决策和任务进度；小说记忆库负责人物、事件、设定和因果事实。已确认作品事实不得只存在于会话摘要中。

#### P1：输出 token 与章节字数

- [ ] 取消普通写作会话固定 `8192` token 的单一输出上限。推理模型可能从同一输出预算中消耗推理 token，固定约 8K 不能稳定保证生成完整的 2500 中文字章节。
- [ ] 将“目标中文字数”和“模型输出 token 上限”拆开配置：用户填写的是目标正文汉字数，系统根据模型、供应商、是否包含推理 token、提示词附加格式和安全余量换算请求上限。
- [ ] 写章任务默认目标应按“约 2500 个中文汉字正文”理解，而不是 2500 字符或 2500 token；生成完成后按可见正文汉字数验收，明显不足时允许在保持情节连续的前提下续写补齐。
- [ ] 输出预算从模型上下文中按真实请求值扣除，替换“统一预留上下文窗口 10%”的粗放算法。普通对话、章节写作、结构化工具、审查任务分别设置预算。
- [ ] 在模型配置中增加能力字段：上下文窗口、最大输出 token、推理 token 是否与输出共用预算、推荐章节输出上限；用户自定义模型允许手动覆盖并进行范围校验。
- [ ] 对长章节采用“单次完整生成优先，受限模型分段续写兜底”的策略；分段时共享章节细纲、上一段尾部和状态约束，最终合并后执行重复段落、断裂和字数检查。

#### P2：存储、性能与验证

- [ ] 将整段会话、全部章节草稿移出单个 `localStorage` JSON，迁移到 SQLite 或 IndexedDB，并提供分页读取、原始消息恢复和容量异常提示。当前 `conversation-store.ts` 只有 13 行，仅存 `currentId`，消息全量在 localStorage，是百万字场景的真实瓶颈。（2026-07-25 代码审查确认）
- [ ] 会话请求改为发送会话 ID、检查点和检查点之后的增量消息，不再由前端每次上传全部历史。
- [ ] 长会话消息列表增加分页或虚拟化，避免消息数量增长后 DOM、序列化和切换会话卡顿。
- [ ] 建立长篇压力测试：至少覆盖 100 万字、300 章、长会话、多次检查点合并、摘要漂移恢复、2500 汉字章节达标率，以及人物/伏笔/关系跨 100 章召回。
- [ ] `gateway.ts` 的频率限制器是进程内 Map，重启后清零，所有 channel 共享同一计数器 `local:channel`，实际是服务级而非用户级限制；本地单用户可接受，Web 化时必须换成持久化计数器（Redis 或 SQLite）。（2026-07-25 代码审查发现）
- [ ] `serverVault.ts` 当前仅组装 `activeForeshadows`（伏笔）+ `recentTimeline`（时间线），缺少人物状态、势力关系、场景地点等核心记忆。在四级记忆体系实现前，Vault 只是 thin wrapper，作为"小说事实真源"的定位尚未落实。（2026-07-25 代码审查发现）
- [ ] `context-compressor.ts` 的"摘要化"是将旧消息整块截断并用结构化检查点替代，不是 LLM 语义压缩；对话中的人物描写、临时剧情判断、用户隐性偏好在截断后永久丢失。长篇真实写作前需升级为 LLM 驱动的语义摘要（参考 grok-build 的 compaction_transcript 方案）。（2026-07-25 代码审查发现）

### 章节正文行内修订方案

> 架构决定：用户在文件树打开完整章节并选中文字后，可以调用 AI 给出局部修改建议。该能力不由 Pi 接管，也不直接复用会话写作 Agent 的完整记忆；在现有 `RefineAgent` 中新增隔离的 `inline_revision` 模式，界面上不新增 Tab。

#### Agent 职责边界

- [ ] 将 `RefineAgent` 拆分为两个执行模式：`chapter_refine` 负责完整章节润色，`inline_revision` 负责选中文字的建议、润色、精简、扩写、对话增强、节奏调整和自定义改写。
- [ ] 会话写作 Agent 继续负责章节创作和续写，不承接高频局部修订，避免会话临时讨论和长历史污染正文编辑。
- [ ] Pi 只负责跨文件审查、批量修改和项目级操作，不参与普通行内润色；只有用户明确发起跨章节统一修改时才进入 Pi 授权链路。
- [ ] 行内修订走功能区 `creationTool` 模型配置，不使用会话区 `chatAgent` 或 Pi 模型，继续遵守现有模型路由隔离。

#### 交互与写入安全

- [ ] 用户选中文字后显示轻量浮动工具条，至少提供：修改建议、润色、精简、扩写、增强对话、调整节奏和自定义要求。
- [ ] 默认采用“建议并改写”：简要说明问题并提供可替换文本；同时支持只给建议、不产生替换。
- [ ] Agent 返回结果必须先显示原文/建议文本差异，提供“替换原文、插入到下方、再生成、补充要求、取消”，未经用户确认不得修改章节文件。
- [ ] 应用修改前创建可恢复快照，并支持撤销；行内修订结果和用户确认动作应进入章节修改记录。
- [ ] 请求携带 `novelId`、`chapterId`、`chapterVersion`、选区起止位置、原文及原文哈希。应用前重新校验版本和哈希；章节已变化时禁止按旧坐标覆盖并要求重新生成。

#### 上下文与记忆隔离

- [ ] 行内修订默认只读取：选中文字、前后 1～3 段、当前章节细纲、本书文风配置、用户本次要求，以及选区明确涉及的人物/势力/能力资料。
- [ ] 禁止向行内修订注入会话全部历史、其他作品消息、Pi 工作记忆、不相关章节或整本小说正文。
- [ ] 把章节原文作为不可信数据边界包裹后再传入提示词，防止正文中的指令性文本改变 Agent 规则。
- [ ] `inline_revision` 使用独立输出协议，不复用当前“返回润色后完整章节”的 JSON；输出至少区分建议、替换文本、修改类型和可能影响的作品事实。

#### 保存后的事实同步

- [ ] 不在每次行内改写后立即更新 Vault；用户确认保存章节后，再对本次修改统一执行轻量语义差异分类。
- [ ] 仅表达、语病、节奏或措辞变化不更新人物、伏笔和故事图谱。
- [ ] 涉及人物状态、人物关系、势力归属、时间地点、能力物品、事件结果或伏笔变化时，生成结构化记忆更新候选。
- [ ] 行内修订 Agent 无权直接写入 Vault；事实变化候选交给状态更新/关系提取链路，与既有设定冲突时必须展示给用户确认。
- [ ] 章节文件始终是正文事实源；Vault、阶段细纲和故事图谱属于可追溯的派生记忆，保存后的同步失败不得回滚或覆盖用户正文。

### 角色生成器与中立命名引擎

> 架构决定：产品不得把开发者个人审美、名字示例或偏好固化为默认标准。命名系统应题材中立、作品隔离、可由每位用户塑形；开发者示例只能作为内部多风格测试集的一部分，不进入正式提示词、用户项目或默认展示。

#### 命名生成管道

- [ ] 将角色生成器内部拆为“角色骨架 → 命名候选生成 → 确定性去重与风险检查 → 命名审查 → 完整角色卡”，避免一次调用同时生成姓名和全部复杂人设导致命名退化为高频模板。
- [ ] 内部先生成较大的候选池，再向用户展示少量通过检查的候选；候选池不直接写入作品文件或 Agent 长期记忆。
- [ ] 命名能力作为角色生成器内部阶段，不新增用户可见 Tab；允许单独换名、保留姓氏换名、保留名字换姓、锁定某个字和查看备选。
- [ ] 为主角、核心对手、重要配角、普通配角和路人设置不同的“命名辨识度预算”，避免全员使用主角级特殊姓名而产生新的 AI 感。

#### 作品级命名画像

- [ ] 从题材、时代、地域/文化、世界观和已有角色动态推导当前作品的临时命名画像；用户可以确认、修改或关闭自动推导。
- [ ] 支持用户配置写实/风格化程度、名字长度、常见/低撞名倾向、偏好结构、允许的复姓/特殊姓氏/艺名/代号、偏好字、禁用字，以及喜欢和不喜欢的名字示例。
- [ ] 用户示例只用于提取当前作品的风格特征，禁止直接照抄或机械重组；用户拒绝的名字只进入当前作品禁用列表，默认不得跨作品学习。
- [ ] 命名画像属于作品级派生配置，与会话写作 Agent、Pi 和其他作品严格隔离；没有用户偏好时保持题材中立，不替用户预设审美。
- [ ] 内置题材模板只定义时代真实性、文化边界和常见姓名结构，例如现代都市、都市异能、古代架空、玄幻仙侠、西幻、科幻星际、民国与现实悬疑，不预置“最好听的名字”。

#### 确定性质量检查

- [ ] 检查与当前作品已有角色完全重名、同名不同姓、同音/近音、字形近似、首字母过近和核心字反复使用；核心人物必须具有足够听觉与视觉区分度。
- [ ] 建立可维护的高频模板组合风险库，用于降低常见 AI 网文姓名组合的重复率；风险库只作为扣分项，不机械封禁单个常用姓氏或汉字。
- [ ] 检查名字与作品时代、地域、族群、家族规则和世界观是否一致；特殊姓名必须有身份或文化依据，不能只为“惊艳”而存在。
- [ ] 分别计算辨识度、音韵、字形可读性、题材适配、现实可信度和用力过猛风险；程序检查与第二阶段模型审查结合，不能仅依赖模型自评。
- [ ] 在生成整组角色时检查群体分布，包括姓氏集中、音节重复、名字长度单一、意象同质化和善恶标签化，避免单个名字合格但整组难以区分。

#### 用户反馈与验证

- [ ] 角色候选卡提供“换名、加入当前作品禁用、设为当前作品风格参考”；所有反馈必须可查看、可撤销、可清空。
- [ ] 对已有作品生成新角色时，自动读取完整角色姓名索引进行去重，不依赖被截断的角色 Markdown。
- [ ] 建立多题材、多审美内部测试集，覆盖写实都市、风格化都市、玄幻仙侠、古代、科幻、西幻等方向；开发者提供的姓名只可作为其中一个匿名测试样本。
- [ ] 验收指标至少包括：项目内零重名、核心角色近音率、重复关键字率、用户首次接受率、连续多批生成的模板复用率，以及题材/时代不适配率。

## 维护规则

- 完成一项后将 `[ ]` 改为 `[x]`，并在必要时补充完成日期。
- 新的架构决定优先写入“已确认的核心架构”。
- 新想法先写入对应章节，不立即扩大实现范围。
- 每次准备灰度、发布 Web 版或打包 EXE 前，重新检查本清单。
