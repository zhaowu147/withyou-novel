# WithYou Novel 项目状态基线

更新时间：2026-08-01

这是本次迁移后建立的项目事实快照。它不依赖历史聊天内容；如果本文与旧交接文件或旧对话冲突，以当前磁盘代码和磁盘变更账本为准。

## 1. 迁移边界

本次迁移执行了只读扫描：仓库结构、Git 分支与提交、`.project` 账本、交接文档、产品清单、核心 Agent/记忆/Pi/图谱/UI/打包代码和 API 路由。

本次没有重跑生产构建、完整测试、真实模型调用或灰度验证，因此旧文档中的“已验收”只作为历史记录，不作为本次新验收结论。

## 2. 磁盘事实状态

项目事实来源固定为：

```text
当前磁盘代码 > .project/ 变更账本 > PROJECT_STATE.md > Git辅助信息 > 对话上下文
```

Git 没有及时提交不影响项目事实；未记录的大改动属于流程缺陷，需要立即补建变更单。

## 3. 仓库辅助状态

```text
当前分支：main
本地基线：c409526
迁移时 origin/main：6d19a79
工作树：存在尚未提交的活动变更（以当前磁盘为事实）
远端同步：本地多一个项目治理提交，尚未推送
```

仓库当前只有 `main`。Git 只用于辅助查看提交和分支，不作为代码是否完成、是否最新或是否安全的判断依据。不要再次清空历史或强制重建主分支。

## 4. 已确认的代码能力

### 3.1 Agent Runtime

入口位于 `src/lib/agents/runtime/`，核心 Agent 为：

- `planner`：剧情规划
- `writer`：章节写作/精修，主要通过 `/api/chat`
- `reviewer`：逻辑、设定、人物和伏笔审查
- `memory`：识别记忆并暂存候选

Writer 的单次模型输出预算为 30,000 tokens；该预算同时在 `/api/chat` Writer 入口和 Runtime 默认值中生效，避免 2,500 汉字以上的章节被旧的 12,000 token 默认值截断。

当 Agent 在工具读取后遇到临时网络错误时，Runtime 会保留同一轮 Tool loop 的工具调用和结果，再以该证据继续生成；不会因重试重复扫描文件树。Step 直连模型的首包和长响应等待时限分别为 5 分钟与 15 分钟。该恢复机制已完成静态检查，真实 100 章验收仍由用户暂停。

Writer 的长正文完成条件是“至少一次成功的证据读取加非空正文”，不要求调用 `agent_done`。该工具标记仅是 Planner、Reviewer、Memory 等结构化 Agent 的完成协议；此前将它用于 Writer 会把已经读取资料并输出正文的正常响应误判为失败。

Writer 的 Tool loop 使用 OpenAI 兼容 SSE 流式解析正文与 tool call 参数，不再等待上游把长正文组装成单个 JSON 响应。临时连接错误、响应体中断、HTTP 429 与 5xx 会进行有界重试，重试沿用已读取证据。项目开发和测试运行时要求 Node 22 或更高版本。

运行时已经具备：

- 当前小说范围的上下文计划
- 工具读取轨迹
- Agent 运行记录和状态
- 证据读取协议
- `agent_done` 完成信号
- Memory 候选审批前不进入正常召回

当前 Agent 运行记录还包含统一任务阶段、子阶段、0-100 进度、尝试次数、ownerId、最近心跳和 checkpoint；`/api/agents/run` 支持进度事件流，Agent 工作台可显示任务进展。Reviewer 可选择综合、设定、节奏或连续性审查视角。

### 3.2 记忆系统

核心代码位于 `src/lib/memory/`：

- 作品级 `canonical`、`long`、`short` 三层记忆
- 章节记忆抽取和自动跟踪
- 导入作品的回填任务
- 5 章剧情块、10 章阶段汇总
- 来源路径、章节、哈希、置信度、状态和版本
- provenance depth，防止派生摘要继续喂给同层摘要
- 每部小说独立 SQLite FTS5 派生索引
- 索引损坏隔离并从 JSON 事实源重建
- 记忆候选的批准、拒绝、调整和停用接口

记忆接口可按需返回按状态和层级汇总的摘要；伏笔接口同时区分已存储、埋下、激活、回收、废弃和待回收数量。原有数组返回形态保持不变。

事实源仍然是小说文件和正文，不是索引、图谱或阶段汇总。

### 3.3 Pi

`src/lib/pi/` 已存在：

- 普通 Pi 会话
- 源码 Pi
- 默认 coding Agent 会话
- 候选改动、批准、回滚
- 工具白名单和请求授权校验
- Skill 原型文件 `source-skill.ts`
- 源码 Pi Skill 管理：来源、版本、内容哈希、可选签名校验、安装、启用/禁用、卸载和完整性隔离
- 普通 Pi 仅加载当前小说 `.skills/` 中有效的项目专属 Skill；不加载全局、其他小说或源码维护 Skill，且拒绝符号链接、越界和超过 64KB 的内容
- 默认 Pi 已接入 SDK coding tools：受控 `read`/`grep`/`find`/`ls`/`bash` 与 `coding_edit` 候选补丁；bash 固定在源码工作区，限制开发命令、超时、输出上限、进程树终止并清理敏感环境变量
- 首次使用可从 Pi 面板检查/安装 Node.js 22、npm/pnpm、Git、Python 3.11+，按锁文件安装项目依赖并创建 `.withyou-python`；Windows 优先走用户级 winget，结果通过 `/api/pi/environment` 返回
- `/api/pi` 现在默认进入 coding runtime；旧 source permissions/route 仅保留兼容入口，不再要求授权口令、grant token、时效或三级选择。

源码 Pi 独立 sidecar 进程形态尚未实现；现有源码 Pi 仍由应用服务端运行时托管。上述能力尚未进行真实权限与安装场景验收。

### 3.4 故事图谱

`src/lib/graph/` 和 `src/app/api/graph/` 已存在：

- 图谱 v3 持久化
- 阶段、图层、节点、关系和事件
- AI 提取进入候选层
- 确认、驳回、隐藏、恢复和恢复点
- 多对多关系和重复关系
- 实体正式名/别名对账
- 图谱 API 与工作区图谱 UI

图谱最终 UI 对账和集中验收仍延期。

当模型在长篇结构化提取中返回空文本或无法解析的 JSON 时，图谱提取会在 90 秒内降级为从文件树明确事实导出的候选节点；不会伪造关系或事件。该路径已用 100 章验收小说验证为可返回图谱节点，模型提取质量与图谱 UI 最终对账仍待后续集中验收。

### 3.5 功能区与聊天区

- 功能区模型与聊天模型路由分开。
- 工具上下文由服务端从当前小说文件树构建，不信任浏览器传入的上下文。
- 拆书接口为 `/api/book-analysis`，属于独立功能链路。
- Agent 工作台提供 Planner、Reviewer、Memory 的运行、轨迹和候选审批。
- `/api/novels/[id]/chapters/[number]/review` 提供基于当前文件树的章节审查入口，审查只读并返回运行记录、证据轨迹和下一步精修提示，不直接覆盖正文。
- ChatPanel 处理写作 Agent 流式响应和工具读取进度。
- 已绑定作品的章节读取和保存采用内容哈希乐观并发保护；保存时发现外部更新会拒绝覆盖并保留当前编辑内容。
- 写作提示词已分为不可违反的事实/权限/输出边界与可调整的商业编辑目标；字数、钩子密度、对话比例、固定角色字段和固定节拍不再作为生成阶段的废稿门禁。写作与精修提示词加入商业网文主编、当代网文读者和追读动力检查。

### 3.6 Electron 交付

- `electron/main.js` 启动本地 Next standalone 服务。
- 打包后数据目录使用 Electron `userData/data`。
- Electron 安装包附带 `coding-environment.json` 环境清单；系统 Git/Python 不打进 asar，由首次使用的环境管理器按用户选择准备。
- 运行时从用户数据目录读取 `.env.local`。
- `scripts/prepare-electron-runtime.mjs` 会移除 standalone 中的外部链接并检查必要文件。
- 当前 ZIP 交付物不属于源码事实，不应提交 Git。

### 3.7 语义对齐与功能区填充

- `src/lib/semantic-alignment/` 提供 Parser、Gate、契约版本、锁定快照、来源过期检测、执行领取锁和 Validator；正式生成必须携带已确认且未过期的契约。
- 语义卡片允许用户编辑任务目标、解释、约束、歧义、假设和长期规则；高风险歧义（包括“冷漠但不要突然”等内在动机缺口）会阻断确认和执行。
- 功能区原“AI帮我填”文字入口已改为带无障碍名称的 Sparkles 图标；服务端一次回读项目上下文、一次模型调用返回全部空字段，严格按模板 schema 过滤并保护已有值。
- 本地会话首次直达增加 `ensureConversation` 兜底，避免路由导航与 localStorage 写入时序导致工作区租约未建立。
- 提示词包写入、SSE 消费、语义契约和工作区绑定均具备原子写入、损坏恢复、超时、取消、版本/哈希和跨进程锁保护。

## 5. 当前明确未完成或待验证

以下事项不能因为代码文件存在就视为完成：

- Agent 证据轨迹、记忆候选批量审批的完整用户流程验收。
- 跨会话、跨作品、路径穿越、上传伪造、Pi coding Agent 和恢复点安全回归。
- 会话语义压缩、消息分页和百万字长篇压力测试。
- 输出 token 预算和中文正文长度验收。
- 行内修订独立模式的真实用户流程验收。
- 角色命名分阶段生成与多题材验收。
- 源码 Pi sidecar 进程形态及 Pi Skill 管理的真实安装验收；coding tools 已完成静态路径/命令策略验收，仍需真实安装包上验证 winget 和项目依赖安装。
- Agent 任务已具备 stale 运行检测、重放参数持久化和下一次同工作区请求触发的自动恢复；它会重新执行任务，不会恢复到上一次模型工具循环的中间 token。应用启动扫描、跨进程恢复和真实中断场景尚未验收。
- UI 最终整理、图谱最终对账、灰度验证。

产品清单 `PRODUCT_EVOLUTION_CHECKLIST.md` 仍保留历史 backlog，后续必须按本基线逐条复核，不能直接把其中的旧 `[ ]` 或 `[x]` 当作当前事实。

## 6. 当前验证记录

- `pnpm project:status`：本次迁移已运行并通过。
- CHG-2026-004：Agent 进度事件、Reviewer 视角、章节审查入口和记忆/伏笔状态摘要已写入磁盘，尚未集中验证。
- CHG-2026-004：写作提示层已完成“事实硬约束 / 商业编辑软约束”重构，尚未进行真实模型盲测。
- 磁盘变更账本：CHG-2026-004（Agent 运行与提示层）和 CHG-2026-005（核心交付缺口）处于活动状态，均按用户要求待集中验证。
- CHG-2026-005：源码 Pi Skill 管理、普通 Pi 项目 Skill 隔离、章节内容版本哈希保护和角色生成阶段化提示已写入磁盘；未运行测试、构建或真实模型调用。
- Git 工作树：仅作辅助信息，不能替代磁盘状态。
- CHG-2026-006：在 Node 22.23.1、本机 HTTP API 与真实模型下完成后端验收。独立小说 `验收玄幻_20260729060006` 已保留供前端复核：100 章、每章至少 2504 中文字符；角色生成、Writer、Planner、Reviewer、Memory、章节哈希冲突、工作区隔离、Pi observer、图谱刷新与前端可读 Agent runs 均有对应 API 证据。该 100 章正文是固定场景重复夹具，仅能证明数据链路，不是长篇叙事质量或 Agent 续写能力的验收。
- CHG-2026-006：长篇图谱模型两次返回空文本，已改为 90 秒上限后降级提取文件树明确事实；最终一次返回 24 个候选节点。该验收小说图谱关系与事件为 0，属于模型结构化输出不可用时的已知质量限制，而非图谱 API 失败。
- CHG-2026-009：使用 Node 22.23.1 直接运行全量测试 150 项、149 通过、0 失败、1 个既有 Windows 符号链接跳过；TypeScript 检查通过；Next 生产构建通过；`scripts/prepare-electron-runtime.mjs` 通过；环境状态烟测已探测 Node 22.23.1、npm 10.8.2、pnpm 10.32.1、Git 2.54.0、Python 3.13.5 和项目依赖。默认 Node 20 入口会触发既有 undici `markAsUncloneable` 兼容错误，因此验收命令固定使用 Node 22。
- CHG-2026-010：默认 Pi API 与面板不再分级，coding workspace 无 token/口令即可用；Node 22.23.1 全量测试 151 项、150 通过、0 失败、1 个既有 Windows 符号链接跳过；生产构建通过。
- CHG-2026-008：Node 22.23.1 下 `pnpm test` 为 148 tests / 147 pass / 0 fail / 1 skipped；`pnpm build` 完成，TypeScript 检查通过；后端验收覆盖契约解析、风险阻断、版本与来源过期、单轮表单填写、正式生成、执行验证、重复领取和隔离失败路径，报告为 `tmp/semantic-backend-acceptance-20260801080742.json`（23 个记录项，0 失败）。
- CHG-2026-008：浏览器已走新会话、工具面板和图标点击；修复本地代理将 localhost 与 127.0.0.1 混用时的来源误判后，工作区隔离可正常建立。当前本机外部模型请求未返回可用结果，因而不把无效凭证/网络条件下的外部模型输出冒充为 UI 成功证据。

## 7. 下一步规则

下一次大改动必须先创建或选择一个变更单：

```text
.project/changes/active/CHG-YYYY-NNN-short-name/
```

在变更单中记录目标、磁盘基线、涉及区域、预计修改文件、实际修改文件、验证结果和遗留问题。完成后移动到 `merged/`；放弃则移动到 `discarded/`，不在当前工作树保留废弃代码。
